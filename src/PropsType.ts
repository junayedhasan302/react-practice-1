export default interface PropsType {
  productName: string;
  price: number;
  inStock: boolean;
}

export interface ItemCartType {
  itemCount: number;
}

export interface greetingType {
  username?: string;
}